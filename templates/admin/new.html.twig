{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}
{% block body %}
    <h1>Create new User <a href="{{ path('app_home') }}">/</a></h1>
    <hr />

    {{ include('admin/_form.html.twig') }}

    <a href="{{ path('app_admin_index') }}">back to list</a>
{% endblock %}
